pluginManagement {
    repositories {
        maven {
            name = 'Saps Mirror'
            url = uri('https://maven.saps.dev/static/mirror/')
        }
        maven {
            name = 'NeoForged'
            url = uri('https://maven.neoforged.net/releases')
        }
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven {
            name = 'Saps Mirror'
            url = uri('https://maven.saps.dev/static/mirror/')
        }
        maven {
            name = 'NeoForged'
            url = uri('https://maven.neoforged.net/releases')
        }
        mavenCentral()
    }
}

rootProject.name = 'music-control-neoforge'